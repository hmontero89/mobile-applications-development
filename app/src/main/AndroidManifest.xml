<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.masterd.helloworld">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_hm"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_hm"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".OtherActivity" />
        <activity android:name=".YetAnotherActivity" />
        <activity android:name=".AddNumbersActivity" />
        <activity android:name=".LoggerActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".OneActivity" />
        <activity android:name=".TwoActivity" />
    </application>

</manifest>